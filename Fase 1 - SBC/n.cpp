#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(){
    ll n, k, l;

    cin >> n;

    ll f[n], v[n];

    for(ll i = 0; i < n; i++){
        cin >> f[i];
    }
    for(ll i = 0; i < n; i++){
        cin >> v[i];
    }

    cin >> k >> l;

    return 0;
}
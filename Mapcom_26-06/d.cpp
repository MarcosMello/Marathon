#include <bits/stdc++.h>

typedef long long ll;
#define FASTIO ios_base::sync_with_stdio(0); cin.tie(0);

using namespace std;

int main(){
    FASTIO;

    vector<ll> dGT;
    vector<ll> dTB;
    vector<pair<ll, string>> ips;

    ll n;

    string x;

    while (cin >> x){
        if(x == EOL){
            break;
        }

        cout << x << "\n";
    }

    return 0;
}